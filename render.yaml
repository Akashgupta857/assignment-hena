services:
  - type: web
    name: book-management-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npx json-server --host 0.0.0.0 --port $PORT db.json
    healthCheckPath: /books
